# Core
# Note: Python 3.9+ is required (install separately)
numpy>=1.23
pandas>=1.5
scikit-learn>=1.1
xgboost>=1.6
joblib>=1.2

# Geospatial (optional for full pipeline)
geopandas>=0.12
rasterio>=1.3
rioxarray>=0.14
xarray>=2023.6
netcdf4>=1.6.0
h5py>=3.8  # Required for reading MODIS HDF files with xarray
pyhdf>=0.10.3  # Required for reading MODIS HDF-EOS files and extracting geolocation
# Note: pyhdf requires system HDF4 libraries. On Windows, may need to install from conda-forge
# or use pre-built wheels. Alternative: use GDAL with HDF4 support.

# Visualization
matplotlib>=3.5
seaborn>=0.12

# Utilities
pytest>=7.0
pyyaml>=6.0
requests>=2.28
python-dotenv>=1.0
pystac-client>=0.7
planetary-computer>=0.5
tqdm>=4.64
shapely>=2.0
rtree>=1.0
pyarrow>=10.0
Pillow>=9.0

# Deep Learning (for extract_embeddings.py)
torch>=2.0
torchvision>=0.15

# Optional / scaling
dask[complete]>=2023.6
lightgbm>=3.3
catboost>=1.1
